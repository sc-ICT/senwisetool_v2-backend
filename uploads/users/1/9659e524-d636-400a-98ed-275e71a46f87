"use client";

import { FaTriangleExclamation } from "react-icons/fa6";
import Button from "@/ui/components/buttons/button";

interface DeleteModalProps {
  message: string;
  isLoading: boolean;
  onCancel: () => void;
  onConfirm: () => void;
}

export function DeleteModal({ message, isLoading, onCancel, onConfirm }: DeleteModalProps) {
  return (
    <div
      className="fixed inset-0 bg-black/60 flex items-center justify-center z-50 backdrop-blur-sm"
      onClick={onCancel}
    >
      <div
        className="bg-[#0d1b35] border border-white/10 rounded-2xl p-6 w-[400px] max-w-[92vw] shadow-2xl shadow-black/50"
        onClick={(e) => e.stopPropagation()}
      >
        {/* Icône avertissement */}
        <div className="flex items-start gap-4 mb-5">
          <div className="w-10 h-10 rounded-xl bg-red-500/10 border border-red-500/20 flex items-center justify-center shrink-0 mt-0.5">
            <FaTriangleExclamation className="w-4 h-4 text-red-400" />
          </div>
          <div>
            <h2 className="text-sm font-semibold text-zinc-100 mb-1">
              Confirmer la suppression
            </h2>
            <p className="text-sm text-zinc-400 leading-relaxed">{message}</p>
          </div>
        </div>

        <div className="flex justify-end gap-2 pt-4 border-t border-white/6">
          <button
            onClick={onCancel}
            disabled={isLoading}
            className="h-9 px-4 text-sm rounded-xl border border-white/10 text-zinc-400 hover:bg-white/5 transition-colors"
          >
            Annuler
          </button>
          <Button
            onClick={onConfirm}
            isLoading={isLoading}
            className="h-9 px-4 text-sm rounded-xl bg-red-600 text-white font-medium hover:bg-red-500 transition-colors"
          >
            Supprimer
          </Button>
        </div>
      </div>
    </div>
  );
}
